const htmlAttributes = require('./attributes')
const cssProperties = require('./css-properties')
const keyboard = require('./keyboard')
const selectors = require('./selectors')

module.exports = {
    htmlAttributes,
    cssProperties,
    keyboard,
    selectors
}

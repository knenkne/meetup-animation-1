import styled from '@emotion/styled'

export const ProductIconStyled = styled.div`
    margin-right: 15px;
    margin-top: 4px;
    position: relative;
    min-width: 36px;
    z-index: 2;
    & > span > svg {
      width: 36px;
      height: 36px;
    }
`

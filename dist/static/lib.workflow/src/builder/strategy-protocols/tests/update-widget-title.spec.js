import { createMockStore } from '../../../tests/utils'

import { storeMock } from './fixtures/store.mock'

describe('Protocols: updateWidgetTitle', () => {
    test('should update widget title for strategy owner widget', () => {
        const store = createMockStore(storeMock.workflow, storeMock.form)

    })
})
